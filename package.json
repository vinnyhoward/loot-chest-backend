{
  "name": "loot-chest-backend",
  "module": "index.ts",
  "type": "module",
  "version": "1.0.0",
  "description": "Backend for Loot Chest",
  "main": "index.ts",
  "scripts": {
    "start": "node index.ts",
    "dev": "bun --hot run index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/figlet": "^1.5.8"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "figlet": "^1.7.0"
  }
}